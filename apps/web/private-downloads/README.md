Place `squaads-extension-internal.zip` here when the web server should expose the internal extension download at `/downloads/squaads-extension-internal.zip`.

This is the single canonical location for the internal extension package because serving that ZIP is a web-only concern.
